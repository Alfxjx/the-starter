{
  "name": "the-starter",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "boot": "lerna bootstrap",
    "server:dev": "lerna run --scope home-admin-server start:dev --stream",
    "cafe:dev": "lerna run --scope home-cafe-menu dev --stream"
  },
  "devDependencies": {
    "lerna": "^5.1.8"
  }
}
