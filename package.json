{
  "name": "the-starter",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "boot": "lerna bootstrap",
    "server": "lerna run --scope home-admin-server start --stream"
  },
  "devDependencies": {
    "lerna": "^5.1.8"
  }
}
